# Examples

This directory contains sample applications that demonstrate how to use the framework in practical scenarios. Each subdirectory focuses on a particular use case or integration pattern.

Some examples require a PostgreSQL database. For these examples, Docker must be installed and the shared database container must be started from the `examples/` directory:

```bash
docker compose up -d
```